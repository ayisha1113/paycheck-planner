<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Couple Finance Calculator</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600;9..40,700&family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&display=swap" rel="stylesheet">
<style>
:root {
  --ink:      #1A1A1A;
  --cream:    #FAF9F6;
  --white:    #FFFFFF;
  --pink:     #FF385C;
  --pink-soft:#FFF0F3;
  --pink-deep:#E0002A;
  --gray-50:  #F7F7F5;
  --gray-100: #EFEFEC;
  --gray-200: #DEDEDE;
  --gray-400: #999999;
  --gray-600: #666666;
  --gray-800: #333333;
  --green:    #1A7A28;
  --green-bg: #F0FAF0;
  --amber:    #B45309;
  --amber-bg: #FFFBEB;
  --red:      #CC3300;
  --red-bg:   #FFF2EE;
}
* { margin:0; padding:0; box-sizing:border-box; }
body { font-family:'DM Sans',system-ui,sans-serif; background:var(--cream); color:var(--ink); line-height:1.55; -webkit-font-smoothing:antialiased; font-size:15px; }

/* NAV */
nav { background:var(--ink); height:64px; padding:0 40px; display:flex; align-items:center; justify-content:space-between; border-bottom:3px solid var(--pink); position:sticky; top:0; z-index:50; }
.logo { display:flex; align-items:center; gap:10px; text-decoration:none; }
.logo-mark { width:32px; height:32px; background:var(--pink); border-radius:6px; display:flex; align-items:center; justify-content:center; color:#fff; font-size:18px; line-height:1; }
.logo-name { font-family:'Fraunces',serif; font-size:18px; font-weight:500; color:#fff; letter-spacing:-0.02em; }
.logo-tag { font-size:12px; color:rgba(255,255,255,0.45); padding-left:14px; margin-left:2px; border-left:1px solid rgba(255,255,255,0.15); }

/* HERO */
.hero { background:var(--ink); padding:64px 40px 72px; position:relative; overflow:hidden; }
.hero::before { content:''; position:absolute; top:-80px; right:-100px; width:480px; height:480px; background:radial-gradient(circle,rgba(255,56,92,0.18) 0%,transparent 65%); border-radius:50%; pointer-events:none; }
.hero-inner { max-width:1100px; margin:0 auto; position:relative; }
.eyebrow { font-size:11px; text-transform:uppercase; letter-spacing:0.15em; font-weight:700; color:var(--pink); margin-bottom:16px; display:block; }
.hero h1 { font-family:'Fraunces',serif; font-weight:500; font-size:clamp(38px,5vw,60px); color:#fff; line-height:1.05; letter-spacing:-0.025em; margin-bottom:20px; max-width:760px; }
.hero h1 em { color:var(--pink); font-style:normal; }
.hero p { font-size:17px; color:rgba(255,255,255,0.6); max-width:540px; line-height:1.65; }

/* MAIN */
.main { max-width:1100px; margin:0 auto; padding:52px 40px 100px; }

/* TABS */
.tab-bar { display:flex; border-bottom:2px solid var(--gray-200); margin-bottom:40px; }
.tab-btn { font-family:'DM Sans',sans-serif; font-size:11px; font-weight:700; letter-spacing:0.1em; text-transform:uppercase; padding:14px 26px; border:none; background:transparent; color:var(--gray-400); cursor:pointer; border-bottom:3px solid transparent; margin-bottom:-2px; transition:all 0.15s; }
.tab-btn:hover { color:var(--ink); }
.tab-btn.active { color:var(--pink); border-bottom-color:var(--pink); }
.tab-pane { display:none; }
.tab-pane.active { display:block; }

/* CARDS */
.card { background:var(--white); border:1px solid var(--gray-200); border-top:3px solid var(--ink); border-radius:4px; padding:36px 40px; margin-bottom:20px; }
.card.accent { border-top-color:var(--pink); }
.card-lbl { font-size:10px; font-weight:700; text-transform:uppercase; letter-spacing:0.15em; color:var(--gray-400); margin-bottom:24px; display:flex; align-items:center; gap:12px; }
.card-lbl::after { content:''; flex:1; height:1px; background:var(--gray-100); }

/* PERSON GRID */
.pgrid { display:grid; grid-template-columns:1fr 1fr; gap:28px; }
.phead { display:flex; align-items:center; gap:10px; margin-bottom:18px; padding-bottom:16px; border-bottom:1px solid var(--gray-100); }
.pdot { width:10px; height:10px; border-radius:50%; flex-shrink:0; }
.pdot-a { background:var(--pink); }
.pdot-b { background:var(--ink); }
.phead-text { }
.plabel { font-size:10px; font-weight:700; text-transform:uppercase; letter-spacing:0.1em; color:var(--gray-400); margin-bottom:4px; }
.pname-inp { font-family:'DM Sans',sans-serif; font-size:16px; font-weight:600; border:none; outline:none; background:transparent; color:var(--ink); border-bottom:2px solid var(--gray-200); padding:2px 0; width:140px; transition:border-color 0.15s; }
.pname-inp:focus { border-bottom-color:var(--pink); }
.pname-inp::placeholder { color:var(--gray-400); font-weight:400; }

/* FIELD ROWS */
.frow { display:flex; align-items:center; gap:12px; margin-bottom:12px; }
.frow label { flex:1; font-size:13px; color:var(--gray-600); margin:0; }
input[type="number"],input[type="text"],select { font-family:'DM Sans',sans-serif; font-size:14px; font-weight:500; padding:9px 13px; border:1.5px solid var(--gray-200); border-radius:4px; background:var(--gray-50); color:var(--ink); outline:none; transition:all 0.15s; width:100%; }
input[type="number"]:focus,input[type="text"]:focus,select:focus { border-color:var(--pink); background:var(--white); box-shadow:0 0 0 3px rgba(255,56,92,0.1); }
input.amt { width:128px; text-align:right; }
input.amt-sm { width:104px; text-align:right; }
input.ninp { flex:1; }
input[type="range"] { flex:1; accent-color:var(--pink); cursor:pointer; padding:0; border:none; background:transparent; box-shadow:none; }
input[type="range"]:focus { box-shadow:none; border:none; }
input[type="checkbox"] { accent-color:var(--pink); width:15px; height:15px; cursor:pointer; flex-shrink:0; padding:0; border:none; background:transparent; box-shadow:none; }

/* EXPENSE / ACCOUNT ROWS */
.erow,.arow { display:flex; align-items:center; gap:10px; padding:10px 14px; margin-bottom:6px; border:1.5px solid var(--gray-200); border-radius:4px; background:var(--gray-50); transition:border-color 0.15s; }
.erow:hover,.arow:hover { border-color:var(--gray-400); }
.erow.drag-over { border-color:var(--pink); box-shadow:0 0 0 2px rgba(255,56,92,0.12); }
.erow.dragging { opacity:0.3; }
.dh { color:var(--gray-400); font-size:15px; cursor:grab; user-select:none; flex-shrink:0; line-height:1; }
.dh:active { cursor:grabbing; }

/* TOTAL BAR */
.tbar { display:flex; justify-content:space-between; align-items:center; background:var(--pink-soft); border:1px solid rgba(255,56,92,0.2); border-left:4px solid var(--pink); border-radius:0 4px 4px 0; padding:12px 20px; margin-top:18px; }
.tbar .tl { font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:0.1em; color:var(--pink-deep); }
.tbar .tv { font-family:'Fraunces',serif; font-size:26px; color:var(--pink-deep); }

/* TOGGLE ROW */
.trow { display:flex; align-items:center; gap:10px; padding:10px 14px; background:var(--gray-50); border:1.5px solid var(--gray-200); border-radius:4px; }
.trow label { font-size:13px; color:var(--gray-600); flex:1; cursor:pointer; margin:0; }
.tnote { background:var(--amber-bg); border:1px solid rgba(180,83,9,0.2); border-left:3px solid var(--amber); border-radius:0 4px 4px 0; padding:9px 14px; font-size:12px; color:var(--amber); margin-top:4px; display:none; }
.tnote.show { display:block; }

/* SPLIT */
.spill { font-size:11px; font-weight:800; letter-spacing:0.04em; background:var(--ink); color:#fff; padding:5px 12px; border-radius:100px; min-width:82px; text-align:center; flex-shrink:0; }
.sresult { display:flex; justify-content:space-between; background:var(--gray-50); border:1px solid var(--gray-200); border-radius:4px; padding:12px 18px; margin-top:8px; }
.sa { font-size:14px; font-weight:700; color:var(--pink); }
.sb { font-size:14px; font-weight:700; color:var(--ink); }

/* BUTTONS */
.btn-add { font-family:'DM Sans',sans-serif; font-size:13px; font-weight:600; padding:7px 18px; border:1.5px dashed var(--gray-400); border-radius:4px; background:transparent; color:var(--gray-600); cursor:pointer; margin-top:10px; transition:all 0.15s; }
.btn-add:hover { border-color:var(--pink); border-style:solid; color:var(--pink); background:var(--pink-soft); }
.btn-rm { font-family:'DM Sans',sans-serif; font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:0.05em; padding:4px 10px; border:1.5px solid var(--gray-200); border-radius:4px; background:transparent; color:var(--gray-400); cursor:pointer; transition:all 0.15s; flex-shrink:0; }
.btn-rm:hover { border-color:var(--red); color:var(--red); background:var(--red-bg); }
.btn-primary { font-family:'DM Sans',sans-serif; font-size:15px; font-weight:700; padding:16px 40px; border:none; border-radius:4px; background:var(--pink); color:#fff; cursor:pointer; width:100%; margin-top:12px; transition:all 0.2s; }
.btn-primary:hover { background:var(--pink-deep); box-shadow:0 8px 24px rgba(255,56,92,0.28); transform:translateY(-1px); }
.btn-primary:active { transform:translateY(0); box-shadow:none; }

/* PRIORITY PILL */
.ppill { font-size:10px; font-weight:700; letter-spacing:0.08em; text-transform:uppercase; padding:3px 10px; border-radius:100px; flex-shrink:0; background:var(--gray-100); color:var(--gray-600); border:1px solid var(--gray-200); }
.ppill.fixed { background:var(--pink-soft); color:var(--pink-deep); border-color:rgba(255,56,92,0.25); }
.ppill.p2 { background:var(--ink); color:#fff; border-color:var(--ink); }
.locked { display:flex; align-items:center; gap:10px; padding:10px 14px; margin-bottom:6px; border:1.5px solid var(--gray-200); border-radius:4px; background:var(--gray-50); opacity:0.6; }

/* RESULTS */
.mstrip { display:grid; grid-template-columns:repeat(auto-fit,minmax(150px,1fr)); margin-bottom:28px; border:1px solid var(--gray-200); border-radius:4px; overflow:hidden; background:var(--white); }
.mcell { padding:22px 24px; border-right:1px solid var(--gray-200); }
.mcell:last-child { border-right:none; }
.mlbl { font-size:10px; font-weight:700; text-transform:uppercase; letter-spacing:0.12em; color:var(--gray-400); margin-bottom:8px; }
.mval { font-family:'Fraunces',serif; font-size:28px; font-weight:500; color:var(--ink); line-height:1; letter-spacing:-0.02em; }
.msub { font-size:12px; color:var(--gray-400); margin-top:4px; }
.mcell.mpink .mval { color:var(--pink); }
.mcell.mgreen .mval { color:var(--green); }

.pcgrid { display:grid; grid-template-columns:1fr 1fr; gap:16px; margin-bottom:24px; }
.pcblock { background:var(--white); border:1px solid var(--gray-200); border-top:3px solid var(--ink); border-radius:4px; padding:28px 30px; }
.pcblock.isA { border-top-color:var(--pink); }
.pchead { display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:16px; padding-bottom:14px; border-bottom:1px solid var(--gray-100); }
.pcwho { font-family:'Fraunces',serif; font-size:19px; font-weight:500; color:var(--ink); }
.pcpc { font-size:11px; color:var(--gray-400); font-weight:600; margin-top:2px; }
.pcgross { font-family:'Fraunces',serif; font-size:22px; color:var(--ink); text-align:right; }
.pcgross-lbl { font-size:10px; text-transform:uppercase; letter-spacing:0.08em; color:var(--gray-400); font-weight:700; text-align:right; margin-bottom:2px; }

.pcs { display:flex; justify-content:space-between; align-items:center; padding:8px 12px; border-radius:4px; margin-bottom:4px; font-size:13px; }
.pcs-keep { background:var(--green-bg); }
.pcs-xfer { background:var(--pink-soft); }
.pcs-sur { background:var(--gray-50); border:1px solid var(--gray-200); }
.pcs-none { background:var(--gray-50); }
.pcslbl { font-weight:600; }
.pcs-keep .pcslbl { color:var(--green); }
.pcs-xfer .pcslbl { color:var(--pink-deep); }
.pcs-sur .pcslbl { color:var(--gray-600); }
.pcs-none .pcslbl { color:var(--gray-400); }
.pcsamt { font-weight:700; font-size:14px; }
.pcs-keep .pcsamt { color:var(--green); }
.pcs-xfer .pcsamt { color:var(--pink); }
.pcs-sur .pcsamt { color:var(--gray-600); }
.pcs-none .pcsamt { color:var(--gray-400); }

.wbox,.ebox { display:flex; gap:12px; align-items:flex-start; border-radius:0 4px 4px 0; padding:12px 16px; font-size:13px; margin-bottom:10px; }
.wbox { background:var(--amber-bg); border:1px solid rgba(180,83,9,0.2); border-left:4px solid var(--amber); color:var(--amber); }
.ebox { background:var(--red-bg); border:1px solid rgba(204,51,0,0.2); border-left:4px solid var(--red); color:var(--red); }

.scard { background:var(--white); border:1px solid var(--gray-200); border-top:3px solid var(--gray-200); border-radius:4px; padding:32px 36px; margin-top:24px; overflow-x:auto; }
.shead { font-family:'Fraunces',serif; font-size:24px; font-weight:500; color:var(--ink); margin-bottom:6px; letter-spacing:-0.01em; }
.ssub { font-size:13px; color:var(--gray-600); margin-bottom:24px; }

.empty { text-align:center; padding:80px 0; color:var(--gray-400); }
.empty h3 { font-family:'Fraunces',serif; font-size:26px; font-weight:500; color:var(--gray-600); margin-bottom:8px; }
.empty p { font-size:14px; }

footer { background:var(--ink); color:rgba(255,255,255,0.4); padding:28px 40px; text-align:center; font-size:12px; border-top:3px solid var(--pink); }
footer strong { color:rgba(255,255,255,0.65); }
</style>
</head>
<body>

<nav>
  <a class="logo" href="#">
    <div class="logo-mark">♡</div>
    <span class="logo-name">Couple Finance Calculator</span>
    <span class="logo-tag">Paycheck Planner</span>
  </a>
</nav>

<div class="hero">
  <div class="hero-inner">
    <span class="eyebrow">Financial planning for two</span>
    <h1>Couple Finance <em>Calculator</em></h1>
    <p>Clear, actionable transfer instructions every paycheck — built around your income, your expenses, and your priorities.</p>
  </div>
</div>

<div class="main">
  <div class="tab-bar">
    <button class="tab-btn active" onclick="showTab('inputs')">Income &amp; Expenses</button>
    <button class="tab-btn" onclick="showTab('accounts')">Accounts &amp; Priority</button>
    <button class="tab-btn" onclick="showTab('results')">Action Plan</button>
  </div>

  <!-- TAB 1 -->
  <div id="tab-inputs" class="tab-pane active">

    <div class="card accent">
      <div class="card-lbl">Monthly income</div>
      <div class="pgrid">
        <div>
          <div class="phead">
            <div class="pdot pdot-a"></div>
            <div class="phead-text">
              <div class="plabel">Person A</div>
              <input class="pname-inp" id="name_a" type="text" placeholder="Enter name" value="Person A" oninput="updateSplit()">
            </div>
          </div>
          <div class="frow"><label>Paycheck 1</label><input class="amt" id="a_p1" type="number" value="2600" min="0"></div>
          <div class="frow"><label>Paycheck 2</label><input class="amt" id="a_p2" type="number" value="2600" min="0"></div>
          <div class="frow"><label>Bonus (added to P2)</label><input class="amt" id="a_bonus" type="number" value="0" min="0" placeholder="0"></div>
        </div>
        <div>
          <div class="phead">
            <div class="pdot pdot-b"></div>
            <div class="phead-text">
              <div class="plabel">Person B</div>
              <input class="pname-inp" id="name_b" type="text" placeholder="Enter name" value="Person B" oninput="updateSplit()">
            </div>
          </div>
          <div class="frow"><label>Paycheck 1</label><input class="amt" id="b_p1" type="number" value="3000" min="0"></div>
          <div class="frow"><label>Paycheck 2</label><input class="amt" id="b_p2" type="number" value="3000" min="0"></div>
          <div class="frow"><label>Bonus (added to P2)</label><input class="amt" id="b_bonus" type="number" value="0" min="0" placeholder="0"></div>
        </div>
      </div>
    </div>

    <div class="card">
      <div class="card-lbl">Shared monthly expenses</div>
      <div id="elist"></div>
      <button class="btn-add" onclick="addExp()">+ Add category</button>
      <div class="trow" style="margin-top:16px;">
        <input type="checkbox" id="ov-toggle" onchange="toggleOv()">
        <label for="ov-toggle">Override total manually</label>
        <input class="amt-sm" id="ov-val" type="number" placeholder="amount" style="display:none;" oninput="updateExpTotal()">
      </div>
      <div class="tbar">
        <span class="tl">Monthly shared expense target</span>
        <span class="tv" id="exp-total">$0</span>
      </div>
    </div>

    <div class="card">
      <div class="card-lbl">Personal spend</div>
      <div class="frow" style="margin-bottom:14px;"><label>Total household personal spend</label><input class="amt" id="ptotal" type="number" value="2000" min="0" oninput="updateSplit()"></div>
      <div class="frow" style="margin-bottom:10px;">
        <label id="slabel" style="color:var(--pink); font-weight:600;">Person A's share</label>
        <input type="range" id="slider" min="0" max="100" value="40" step="1" oninput="updateSplit()">
        <span class="spill" id="spill">40% / 60%</span>
      </div>
      <div class="sresult">
        <span class="sa" id="sa-out">Person A keeps: $800</span>
        <span class="sb" id="sb-out">Person B keeps: $1,200</span>
      </div>
      <div class="frow" style="margin-top:16px;">
        <label>Deduct personal spend from</label>
        <select id="pmode" style="width:auto;flex-shrink:0;">
          <option value="p1">Paycheck 1 only (default)</option>
          <option value="split">Split evenly across both paychecks</option>
        </select>
      </div>
    </div>

    <button class="btn-primary" onclick="calculate()">Calculate action plan →</button>
  </div>

  <!-- TAB 2 -->
  <div id="tab-accounts" class="tab-pane">
    <div class="card">
      <div class="card-lbl">Funding priority order</div>
      <p style="font-size:13px;color:var(--gray-600);margin-bottom:20px;">Shared expenses are always funded first. Drag all other accounts — including the sinking fund — to reorder. The account in Position 2 gets the optional partial-fund toggle.</p>
      <div class="locked">
        <span style="flex:1;font-size:14px;font-weight:600;">Shared expense account</span>
        <span class="ppill fixed">Priority 1 · fixed</span>
      </div>
      <div id="alist"></div>
      <button class="btn-add" onclick="addAcct()">+ Add savings account</button>
    </div>
    <button class="btn-primary" onclick="calculate()">Calculate action plan →</button>
  </div>

  <!-- TAB 3 -->
  <div id="tab-results" class="tab-pane">
    <div id="results">
      <div class="empty">
        <div style="font-size:42px;margin-bottom:16px;">📋</div>
        <h3>No plan yet</h3>
        <p>Fill in your income and expenses, then click "Calculate action plan".</p>
      </div>
    </div>
  </div>
</div>

<footer><strong>Couple Finance Calculator</strong> · All calculations run locally — no data is stored or sent anywhere.</footer>

<script>
let expenses=[{id:1,name:'Rent',amount:1800},{id:2,name:'Car loan',amount:400},{id:3,name:'Car insurance',amount:120},{id:4,name:'Electricity',amount:100},{id:5,name:'Groceries',amount:600},{id:6,name:'Restaurants',amount:300}];
let expId=7;
let accounts=[{id:1,name:'Sinking fund',target:500,partial:false},{id:2,name:'Emergency fund',target:300,partial:false},{id:3,name:'Joint savings',target:500,partial:false}];
let acctId=4;
let dSrc=null,dKind=null;

const fmt=n=>'$'+Math.round(n).toLocaleString();
const nA=()=>document.getElementById('name_a').value.trim()||'Person A';
const nB=()=>document.getElementById('name_b').value.trim()||'Person B';
const esc=s=>(s||'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;');

function renderExp(){
  const el=document.getElementById('elist');
  el.innerHTML='';
  expenses.forEach((e,i)=>{
    const r=document.createElement('div');
    r.className='erow'; r.draggable=true; r.dataset.i=i;
    r.innerHTML=`<span class="dh">⠿</span>
      <input type="text" class="ninp" value="${esc(e.name)}" placeholder="Category name" data-f="name" data-i="${i}">
      <input type="number" class="amt-sm" value="${e.amount}" min="0" placeholder="0" data-f="amount" data-i="${i}" style="width:110px;">
      <button class="btn-rm" onclick="rmExp(${i})">Remove</button>`;
    r.addEventListener('dragstart',ev=>{dSrc=i;dKind='exp';r.classList.add('dragging');ev.dataTransfer.effectAllowed='move';});
    r.addEventListener('dragend',()=>r.classList.remove('dragging'));
    r.addEventListener('dragover',ev=>{ev.preventDefault();r.classList.add('drag-over');});
    r.addEventListener('dragleave',()=>r.classList.remove('drag-over'));
    r.addEventListener('drop',ev=>{
      ev.preventDefault();r.classList.remove('drag-over');
      if(dKind!=='exp'||dSrc===null||dSrc===+r.dataset.i)return;
      const m=expenses.splice(dSrc,1)[0];expenses.splice(+r.dataset.i,0,m);
      renderExp();updateExpTotal();
    });
    r.querySelectorAll('input[data-f]').forEach(inp=>{
      inp.addEventListener('input',()=>{
        const idx=+inp.dataset.i,f=inp.dataset.f;
        expenses[idx][f]=f==='amount'?Number(inp.value||0):inp.value;
        if(f==='amount')updateExpTotal();
      });
    });
    el.appendChild(r);
  });
  updateExpTotal();
}
function addExp(){expenses.push({id:expId++,name:'',amount:0});renderExp();document.querySelectorAll('#elist .ninp')[expenses.length-1]?.focus();}
function rmExp(i){expenses.splice(i,1);renderExp();}
function getExpT(){
  if(document.getElementById('ov-toggle').checked){const v=Number(document.getElementById('ov-val').value);if(v)return v;}
  return expenses.reduce((s,e)=>s+Number(e.amount||0),0);
}
function updateExpTotal(){document.getElementById('exp-total').textContent=fmt(getExpT());}
function toggleOv(){const s=document.getElementById('ov-toggle').checked;document.getElementById('ov-val').style.display=s?'inline-block':'none';updateExpTotal();}

function updateSplit(){
  const total=Number(document.getElementById('ptotal').value)||0;
  const pct=Number(document.getElementById('slider').value);
  const a=Math.round(total*pct/100),b=total-a;
  document.getElementById('spill').textContent=pct+'% / '+(100-pct)+'%';
  document.getElementById('slabel').textContent=(nA()+"'s share");
  document.getElementById('sa-out').textContent=nA()+' keeps: '+fmt(a);
  document.getElementById('sb-out').textContent=nB()+' keeps: '+fmt(b);
}

function renderAccts(){
  const el=document.getElementById('alist');
  el.innerHTML='';
  accounts.forEach((ac,i)=>{
    const pri=i+2,isP2=i===0;
    const r=document.createElement('div');
    r.className='arow'; r.style.flexWrap='wrap'; r.draggable=true; r.dataset.i=i;
    r.innerHTML=`<span class="dh">⠿</span>
      <input type="text" class="ninp" value="${esc(ac.name)}" placeholder="Account name" data-f="name" data-i="${i}" style="flex:1;min-width:120px;">
      <label style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:0.07em;color:var(--gray-400);white-space:nowrap;margin:0 6px 0 10px;">Target</label>
      <input type="number" class="amt-sm" value="${ac.target}" min="0" data-f="target" data-i="${i}">
      <span class="ppill ${isP2?'p2':''}">P${pri}</span>
      <button class="btn-rm" onclick="rmAcct(${i})">Remove</button>
      ${isP2?`<div style="width:100%;margin-top:8px;padding-top:10px;border-top:1px solid var(--gray-100);">
        <div class="trow" style="margin:0;">
          <input type="checkbox" id="partial_${ac.id}" ${ac.partial?'checked':''} data-ai="${i}">
          <label for="partial_${ac.id}">Allow partial fund — contribute whatever is available if income falls short</label>
        </div>
        <div class="tnote ${ac.partial?'show':''}" id="pnote_${i}">When enabled: remaining income funds this account partially rather than skipping it entirely.</div>
      </div>`:''}`;
    r.addEventListener('dragstart',ev=>{dSrc=i;dKind='acct';ev.dataTransfer.effectAllowed='move';});
    r.addEventListener('dragover',ev=>{ev.preventDefault();r.classList.add('drag-over');});
    r.addEventListener('dragleave',()=>r.classList.remove('drag-over'));
    r.addEventListener('drop',ev=>{
      ev.preventDefault();r.classList.remove('drag-over');
      if(dKind!=='acct'||dSrc===null||dSrc===+r.dataset.i)return;
      const m=accounts.splice(dSrc,1)[0];accounts.splice(+r.dataset.i,0,m);renderAccts();
    });
    r.querySelectorAll('input[data-f]').forEach(inp=>{
      inp.addEventListener('input',()=>{
        const idx=+inp.dataset.i,f=inp.dataset.f;
        accounts[idx][f]=f==='target'?Number(inp.value||0):inp.value;
      });
    });
    if(isP2){
      const cb=r.querySelector(`#partial_${ac.id}`);
      if(cb)cb.addEventListener('change',()=>{
        const idx=+cb.dataset.ai;
        accounts[idx].partial=cb.checked;
        document.getElementById(`pnote_${idx}`)?.classList.toggle('show',cb.checked);
      });
    }
    el.appendChild(r);
  });
}
function addAcct(){accounts.push({id:acctId++,name:'',target:0,partial:false});renderAccts();document.querySelectorAll('#alist .ninp')[accounts.length-1]?.focus();}
function rmAcct(i){accounts.splice(i,1);renderAccts();}

function showTab(t){
  document.querySelectorAll('.tab-pane').forEach(el=>el.classList.remove('active'));
  document.querySelectorAll('.tab-btn').forEach(el=>el.classList.remove('active'));
  document.getElementById('tab-'+t).classList.add('active');
  ['inputs','accounts','results'].indexOf(t);
  document.querySelectorAll('.tab-btn')[['inputs','accounts','results'].indexOf(t)].classList.add('active');
}

function calculate(){
  const aN=nA(),bN=nB();
  const aP1g=Number(document.getElementById('a_p1').value)||0;
  const aP2g=(Number(document.getElementById('a_p2').value)||0)+(Number(document.getElementById('a_bonus').value)||0);
  const bP1g=Number(document.getElementById('b_p1').value)||0;
  const bP2g=(Number(document.getElementById('b_p2').value)||0)+(Number(document.getElementById('b_bonus').value)||0);
  const totalP=Number(document.getElementById('ptotal').value)||0;
  const pct=Number(document.getElementById('slider').value);
  const aPer=Math.round(totalP*pct/100),bPer=totalP-aPer;
  const mode=document.getElementById('pmode').value;
  const expT=getExpT();

  const bks=[
    {key:'aP1',lbl:aN+' — Paycheck 1',who:'a',gross:aP1g,rem:aP1g,keep:0,xfers:[]},
    {key:'bP1',lbl:bN+' — Paycheck 1',who:'b',gross:bP1g,rem:bP1g,keep:0,xfers:[]},
    {key:'aP2',lbl:aN+' — Paycheck 2',who:'a',gross:aP2g,rem:aP2g,keep:0,xfers:[]},
    {key:'bP2',lbl:bN+' — Paycheck 2',who:'b',gross:bP2g,rem:bP2g,keep:0,xfers:[]},
  ];
  const [bA1,bB1,bA2,bB2]=bks;
  const warns=[];

  if(mode==='p1'){
    bA1.keep=Math.min(aPer,bA1.rem);bA1.rem-=bA1.keep;
    bB1.keep=Math.min(bPer,bB1.rem);bB1.rem-=bB1.keep;
    if(bA1.keep<aPer)warns.push({t:'w',m:`${aN}'s paycheck 1 is ${fmt(aPer-bA1.keep)} short for personal spend.`});
    if(bB1.keep<bPer)warns.push({t:'w',m:`${bN}'s paycheck 1 is ${fmt(bPer-bB1.keep)} short for personal spend.`});
  } else {
    const aH=Math.round(aPer/2),aR=aPer-aH;
    bA1.keep=Math.min(aH,bA1.rem);bA1.rem-=bA1.keep;
    bA2.keep=Math.min(aR,bA2.rem);bA2.rem-=bA2.keep;
    const bH=Math.round(bPer/2),bR=bPer-bH;
    bB1.keep=Math.min(bH,bB1.rem);bB1.rem-=bB1.keep;
    bB2.keep=Math.min(bR,bB2.rem);bB2.rem-=bB2.keep;
  }

  function fund(name,needed,list){
    let f=0;
    for(const bk of list){
      if(needed<=0)break;
      const g=Math.min(bk.rem,needed);
      if(g>0){bk.xfers.push({to:name,a:g});bk.rem-=g;f+=g;needed-=g;}
    }
    return f;
  }

  const expF=fund('Shared expense account',expT,bks);
  if(expF<expT)warns.push({t:'e',m:`Shortfall of ${fmt(expT-expF)} for shared expenses.`});

  const acctSum=[];
  accounts.forEach((ac,i)=>{
    if(ac.target<=0)return;
    const avail=bks.reduce((s,b)=>s+b.rem,0);
    const isP2=i===0;
    let toFund=ac.target;
    if(isP2&&!ac.partial&&avail<ac.target){
      warns.push({t:'w',m:`Not enough for "${ac.name}" (need ${fmt(ac.target)}, available ${fmt(avail)}). Enable "allow partial fund" to use what's available.`});
      acctSum.push({name:ac.name,target:ac.target,funded:0});return;
    }
    if(ac.partial)toFund=Math.min(ac.target,avail);
    const funded=fund(ac.name,toFund,bks);
    if(funded<ac.target)warns.push({t:'w',m:`"${ac.name}": funded ${fmt(funded)} of ${fmt(ac.target)}.`});
    acctSum.push({name:ac.name,target:ac.target,funded});
  });

  const totInc=bks.reduce((s,b)=>s+b.gross,0);
  const totLeft=bks.reduce((s,b)=>s+b.rem,0);
  renderResults(bks,warns,{expF,expT,acctSum,totInc,totLeft,aN,bN});
  showTab('results');
}

function renderResults(bks,warns,sum){
  let h='';
  warns.forEach(w=>{h+=`<div class="${w.t==='e'?'ebox':'wbox'}"><span>${w.t==='e'?'🚨':'⚠️'}</span><span>${w.m}</span></div>`;});

  h+='<div class="mstrip">';
  h+=`<div class="mcell"><div class="mlbl">Total income</div><div class="mval">${fmt(sum.totInc)}</div></div>`;
  h+=`<div class="mcell mpink"><div class="mlbl">Shared expenses</div><div class="mval">${fmt(sum.expF)}</div><div class="msub">of ${fmt(sum.expT)}</div></div>`;
  sum.acctSum.forEach(a=>{h+=`<div class="mcell"><div class="mlbl">${a.name}</div><div class="mval">${fmt(a.funded)}</div><div class="msub">of ${fmt(a.target)}</div></div>`;});
  h+=`<div class="mcell mgreen"><div class="mlbl">Total leftover</div><div class="mval">${fmt(sum.totLeft)}</div></div>`;
  h+='</div>';

  h+='<div class="pcgrid">';
  bks.forEach(bk=>{
    if(bk.gross===0)return;
    const [who,pc]=bk.lbl.split(' — ');
    h+=`<div class="pcblock ${bk.who==='a'?'isA':''}">
      <div class="pchead">
        <div><div class="pcwho">${who}</div><div class="pcpc">${pc}</div></div>
        <div><div class="pcgross-lbl">Gross</div><div class="pcgross">${fmt(bk.gross)}</div></div>
      </div>`;
    if(bk.keep>0)h+=`<div class="pcs pcs-keep"><span class="pcslbl">Keep — personal spend</span><span class="pcsamt">${fmt(bk.keep)}</span></div>`;
    bk.xfers.forEach(t=>{h+=`<div class="pcs pcs-xfer"><span class="pcslbl">→ ${t.to}</span><span class="pcsamt">${fmt(t.a)}</span></div>`;});
    if(bk.rem>0)h+=`<div class="pcs pcs-sur"><span class="pcslbl">Surplus (unallocated)</span><span class="pcsamt">${fmt(bk.rem)}</span></div>`;
    if(bk.keep===0&&bk.xfers.length===0&&bk.rem===0)h+=`<div class="pcs pcs-none"><span class="pcslbl">No action this paycheck</span><span class="pcsamt">—</span></div>`;
    h+='</div>';
  });
  h+='</div>';
  h+=buildSankey(bks,sum.totInc,sum.aN,sum.bN);
  document.getElementById('results').innerHTML=h;
}

function buildSankey(bks,totInc,aN,bN){
  if(!totInc)return'';
  const W=860,H=420,PAD=16,NW=160,NH=30;
  const srcs=bks.filter(b=>b.gross>0);
  const cx1=PAD,cx2=W-PAD-NW;
  const dm={},dord=[],seen=new Set();
  const addD=(k,v)=>{dm[k]=(dm[k]||0)+v;if(!seen.has(k)){seen.add(k);dord.push(k);}};
  srcs.forEach(bk=>{
    if(bk.keep>0)addD('keep_'+bk.key,bk.keep);
    bk.xfers.forEach(t=>addD(t.to,t.a));
    if(bk.rem>0)addD('__sur__',bk.rem);
  });
  const sorted=[...dord.filter(d=>d.startsWith('keep_')),...dord.filter(d=>!d.startsWith('keep_')&&d!=='__sur__'),...dord.filter(d=>d==='__sur__')];
  const sH=Math.min((H-PAD*2)/srcs.length,72),dH=Math.min((H-PAD*2)/sorted.length,62);
  const sY=i=>PAD+i*sH,dY=i=>PAD+i*dH;
  const cols={};const extra=['#FF385C','#7C3AED','#0369A1','#065F46','#92400E','#B91C1C'];let ci=0;
  sorted.forEach(d=>{
    if(d==='Shared expense account')cols[d]='#FF385C';
    else if(d==='__sur__')cols[d]='#1A7A28';
    else if(d.startsWith('keep_'))cols[d]='#666';
    else cols[d]=extra[1+ci++%(extra.length-1)];
  });
  const sOff={},dOff={};
  srcs.forEach((b,i)=>{sOff[b.key]=sY(i)+2;});
  sorted.forEach((d,i)=>{dOff[d]=dY(i)+2;});
  let paths='',sN='',dN='';
  srcs.forEach(bk=>{
    const flows=[];
    if(bk.keep>0)flows.push({dest:'keep_'+bk.key,a:bk.keep});
    bk.xfers.forEach(t=>flows.push({dest:t.to,a:t.a}));
    if(bk.rem>0)flows.push({dest:'__sur__',a:bk.rem});
    flows.forEach(fl=>{
      const di=sorted.indexOf(fl.dest);if(di===-1)return;
      const th=Math.max(2,(fl.a/totInc)*(H-PAD*2-srcs.length*5));
      const x1=cx1+NW,y1=sOff[bk.key]+th/2,x2=cx2,y2=dOff[fl.dest]+th/2;
      sOff[bk.key]+=th;dOff[fl.dest]+=th;
      const mx=(x1+x2)/2,col=cols[fl.dest]||'#999';
      paths+=`<path d="M${x1},${y1-th/2} C${mx},${y1-th/2} ${mx},${y2-th/2} ${x2},${y2-th/2} L${x2},${y2+th/2} C${mx},${y2+th/2} ${mx},${y1+th/2} ${x1},${y1+th/2} Z" fill="${col}" fill-opacity="0.18" stroke="${col}" stroke-width="0.5" stroke-opacity="0.5"/>`;
    });
  });
  srcs.forEach((bk,i)=>{
    const y=sY(i),col=bk.who==='a'?'#FF385C':'#1A1A1A';
    const [person,pc]=bk.lbl.split(' — ');
    sN+=`<rect x="${cx1}" y="${y+1}" width="${NW}" height="${Math.min(sH-4,NH)}" rx="3" fill="${col}" fill-opacity="0.08" stroke="${col}" stroke-width="1.5"/>`;
    sN+=`<text x="${cx1+10}" y="${y+16}" font-family="DM Sans,sans-serif" font-size="11" fill="${col}" font-weight="700">${person}</text>`;
    sN+=`<text x="${cx1+10}" y="${y+29}" font-family="DM Sans,sans-serif" font-size="10" fill="${col}" opacity="0.7">${pc} · ${fmt(bk.gross)}</text>`;
  });
  sorted.forEach((d,i)=>{
    const y=dY(i),col=cols[d]||'#999';
    const lbl=d.startsWith('keep_')?'Personal spend':d==='__sur__'?'Surplus':d;
    dN+=`<rect x="${cx2}" y="${y+1}" width="${NW}" height="${Math.min(dH-4,NH)}" rx="3" fill="${col}" fill-opacity="0.08" stroke="${col}" stroke-width="1.5"/>`;
    dN+=`<text x="${cx2+10}" y="${y+15}" font-family="DM Sans,sans-serif" font-size="10" fill="${col}" font-weight="700">${lbl.length>21?lbl.slice(0,19)+'…':lbl}</text>`;
    dN+=`<text x="${cx2+10}" y="${y+27}" font-family="DM Sans,sans-serif" font-size="10" fill="${col}" opacity="0.7">${fmt(dm[d])}</text>`;
  });
  return`<div class="scard">
    <div class="shead">Money flow</div>
    <div class="ssub">How each paycheck is distributed across all destinations</div>
    <svg viewBox="0 0 ${W} ${H}" width="100%" style="overflow:visible;min-width:480px;">${paths}${sN}${dN}</svg>
  </div>`;
}

renderExp();renderAccts();updateSplit();
</script>
</body>
</html>
